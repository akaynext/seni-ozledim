<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Senin İçin Bir Sürpriz Var!</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            text-align: center;
            background-color: #ffccda;
            overflow: hidden;
        }
        .container {
            margin-top: 100px;
        }
        .envelope {
            width: 120px;
            height: 90px;
            background-color: #ff4d6d;
            position: relative;
            margin: 100px auto;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            animation: shake 0.5s infinite alternate ease-in-out;
            transition: transform 0.3s;
        }
        .envelope:hover {
            transform: scale(1.1);
        }
        .envelope::before {
            content: "";
            position: absolute;
            top: -45px;
            left: 0;
            width: 0;
            height: 0;
            border-left: 60px solid transparent;
            border-right: 60px solid transparent;
            border-bottom: 45px solid #ff4d6d;
        }
        @keyframes shake {
            0% { transform: rotate(-3deg); }
            100% { transform: rotate(3deg); }
        }
        .message {
            display: none;
            font-size: 26px;
            color: #d63384;
            margin-top: 20px;
            animation: fadeIn 1s ease-in-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        .confetti {
            position: fixed;
            width: 12px;
            height: 12px;
            background-color: yellow;
            top: -10px;
            opacity: 0.8;
            animation: fall 2s ease-out forwards;
            border-radius: 50%;
        }
        @keyframes fall {
            0% { transform: translateY(0) rotate(0deg); }
            100% { transform: translateY(100vh) rotate(720deg); }
        }
        .hearts {
            position: fixed;
            font-size: 30px;
            color: red;
            animation: floatUp 4s infinite ease-in-out;
        }
        @keyframes floatUp {
            0% { transform: translateY(100vh) scale(1); opacity: 1; }
            100% { transform: translateY(-100vh) scale(0.5); opacity: 0; }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Bir Sürpriz Mesajın Var! 🎁</h1>
        <div class="envelope" id="envelope"></div>
        <p id="message" class="message">Seni Çooooooook Özledim! 💖</p>
    </div>

    <script>
        document.getElementById("envelope").addEventListener("click", function() {
            this.style.display = "none";
            document.getElementById("message").style.display = "block";
            createConfetti();
            createHearts();
        });

        function createConfetti() {
            for (let i = 0; i < 60; i++) {
                let confetti = document.createElement("div");
                confetti.className = "confetti";
                confetti.style.left = Math.random() * 100 + "vw";
                confetti.style.backgroundColor = `hsl(${Math.random() * 360}, 100%, 50%)`;
                confetti.style.animationDuration = Math.random() * 3 + 1 + "s";
                document.body.appendChild(confetti);
                setTimeout(() => confetti.remove(), 3000);
            }
        }

        function createHearts() {
            for (let i = 0; i < 15; i++) {
                let heart = document.createElement("div");
                heart.className = "hearts";
                heart.innerHTML = "❤️";
                heart.style.left = Math.random() * 100 + "vw";
                heart.style.animationDuration = Math.random() * 4 + 2 + "s";
                document.body.appendChild(heart);
                setTimeout(() => heart.remove(), 4000);
            }
        }
    </script>
</body>
</html>
